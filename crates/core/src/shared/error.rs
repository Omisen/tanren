//! Tipo di errore del core.
//!
//! Il confine con il frontend attraversa i comandi Tauri, quindi l'errore deve
//! poter essere serializzato senza perdere il motivo del fallimento.

// Da definire nello step dedicato: enum degli errori di dominio, storage e
// contenuto, con conversione verso una forma serializzabile per il frontend.
